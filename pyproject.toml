[tool.poetry]
name = "scrapping_and_post_libraries"
version = "0.1.0"
description = ""
authors = ["MLSMIT <manuel.luis.sanchez@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10.14"
hikerapi = "^0.0.2"
pandas = "^2.2.2"
numpy = "^1.26.4"
requests = "^2.32.3"

[tool.poetry.packages]
fb-api = { include = "fb-api" }
ig-api = { include = "ig-api" }
shared-utils = { include = "shared-utils" }
th-api = { include = "th-api" }
tt-api = { include = "tt-api" }
yt-api = { include = "yt-api" }
x-api = { include = "x-api" }
main-app = { include = "main-app" }
tests = { include = "tests" }



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"